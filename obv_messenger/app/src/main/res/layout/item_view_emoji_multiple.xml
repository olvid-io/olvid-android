<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">
    <TextView
        android:id="@+id/emoji_text_view"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:textSize="25dp"
        android:gravity="center"
        android:textColor="@color/black"
        android:theme="@style/SubtleBlueRipple"
        android:background="@drawable/background_circular_ripple"
        tools:text="👩"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:ignore="SpUsage" />

    <ImageView
        android:id="@+id/emoji_variants_corner"
        android:layout_width="8dp"
        android:layout_height="8dp"
        android:src="@drawable/emoji_variants_corner"
        android:layout_marginTop="2dp"
        android:layout_marginEnd="2dp"
        app:layout_constraintTop_toTopOf="@id/emoji_text_view"
        app:layout_constraintEnd_toEndOf="@id/emoji_text_view"
        tools:ignore="SmallSp" />
</androidx.constraintlayout.widget.ConstraintLayout>